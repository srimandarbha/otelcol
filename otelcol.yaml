receivers:
  uptime:
    collection_interval: 5s

processors:
  attributes:
    actions:
      - key: host.name
        value: "$HOSTNAME"
        action: insert

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    metrics:
      receivers: [uptime]
      processors: [attributes]
      exporters: [debug]
